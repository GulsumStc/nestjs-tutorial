import { Controller } from '@nestjs/common';

@Controller('users') // decorator 
export class UsersController {

  /* 
  GET users
  GET users/:id
  POST users
  PATCH users/:id
  DELETE users/:id
   */
}
